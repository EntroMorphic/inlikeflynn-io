# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
*.code-workspace

# ---- working artifacts (not part of the deployed site) ----
screenshots/
*.log

# ---- source material (kept locally / in cloud, not in the repo) ----
# Raw drops that the deployed site does not reference (it uses assets/).
uploads/
*.pdf
*.pptx
*.ppt
*.docx
*.doc

# ---- dependencies (only if a toolchain is ever added) ----
node_modules/
.cache/
dist/
