# Dev Note — 2026-07-16 sync + follow-ups

Quick hand-off so you can align with what landed on `main` today. All of this is
already committed and pushed; nothing here needs redoing — it's context.

## What happened

1. **Your latest build was synced into the repo** (from `inlikeflynn.io.zip`) and
   committed as **`9edf816`** — the full TD2 red-team remediation pass:
   three.js vendored locally + SRI on all 9 grid pages, dead React/Babel/JSX and the
   old pitch pages archived into `backups/` (as git **renames**, not deletes), WebP
   images, JSON-LD on the home page, placeholder headshots removed, `bump-cache.mjs`
   skip-list fixed. See `CHANGELOG.md` for the itemized list.

2. **`.gitignore` now tracks `backups/` and `explorations/`.** Your build removed
   those two lines, and the accompanying `robots.txt` + CI changes confirm the intent:
   these dirs are now **committed and served, but not indexed**. If that was *not*
   intended and you want them local-only again, re-add `backups/` and `explorations/`
   to `.gitignore` and `git rm --cached` them — ping me and I'll do it.

3. **Small adjustments I made on top of your build:**
   - Added `Disallow: /explorations/` to `robots.txt`. Your build disallowed
     `/backups/` but not `/explorations/`, which left the dev-scratch pages crawlable.
     Now both archive dirs are consistently no-index.
   - Added `zip/` to `.gitignore` (the transport folder for your zips) — commit `44d5183`.
   - **Archived three orphaned PNGs** into `backups/` (commit below): the WebP
     conversion left the originals behind with nothing referencing them —
     `screenshot-wide.png`, `screenshot-narrow.png`, `flynn-scope.png` (~2.0 MB).
     Their `.webp` replacements are wired in `site.webmanifest` / `why-flynn.html`.
     If you'd rather serve them as `<picture>` fallbacks for old browsers instead of
     archiving, say so and I'll wire that instead (WebP support is ~97%, so archiving
     was the simpler call).

## Things to know for your next pass

- **Cache version is uniform at `20260716-1943`** across HTML `?v=`, the `var V`
  constants in `site-nav.js`/`site-footer.js`, and `sw.js` `VERSION`. Run
  `node scripts/bump-cache.mjs` as the **last** step before every deploy — CI fails
  if the tags drift. It now correctly skips `backups/`, `explorations/`, `ziptest/`,
  `dist/`.
- **CI scope:** the lychee link check and cache-uniformity check exclude `backups/`
  and `explorations/`. Those archives keep their own historical `?v=` tags and
  root-relative paths on purpose — don't re-stamp them.
- **Team headshots** are still pending: `pages/team.html` has the `<img>` slots
  commented out with drop-in guidance. Drop square photos at
  `assets/img/team/{tripp-josserand-austin,john-josserand,robert-sitton}.jpg`,
  uncomment the tags, and bump the cache version.
- **Remaining image weight** (optional, not on the first-paint path): `og-flynn.png`
  (744 KB) and `icon-512.png` (444 KB) are still unoptimized PNGs — a lossless
  optimizer pass (oxipng/pngquant) would trim them, but neither loads on a normal
  page visit.

## Footprint, for reference

A normal page visit downloads **~888 KB** of local assets (dominated by
`three.min.js` ~594 KB, plus Google Fonts, both browser-cached). The full served
surface is ~13 MB, but ~9.1 MB of that is game music that only loads if someone
unlocks *CYBERSPACE ANOMALOIDS*. Nothing else on the served surface is orphaned.
